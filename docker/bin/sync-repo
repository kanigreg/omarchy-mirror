#!/bin/bash

pacman -Syy
pacman -Syw "$(pacman -Slq omarchy)"

repo-add --new /var/cache/pacman/pkg/omarchy.db.tar.zst /var/cache/pacman/pkg/*.pkg.tar.zst

# Delete packages, except most recent two versions
pacman -S pacman-contrib --noconfirm --needed
paccache -rv -k 2
