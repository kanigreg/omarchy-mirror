#!/bin/bash

OMARCHY_MIRROR="$HOME"/.local/share/omarchy-mirror

sudo cp "$OMARCHY_MIRROR"/nginx/nginx.conf /etc/nginx/
if systemctl is-active --quiet nginx; then
  sudo systemctl reload nginx
else
  sudo systemctl start nginx
fi
